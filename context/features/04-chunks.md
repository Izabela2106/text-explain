### 2. Chunk if needed — programmatic

Add a function that:

- Split long text into paragraphs or sentences before calling the model
- Keep chunks small enough for reliable, cheap analysis
- Preserve enough context so terms are not judged in isolation when possible