### 1. Normalize input — programmatic
Write a function for normalizing user text:

- Trim leading/trailing whitespace
- Normalize line endings
- Reject empty input
- Enforce a max length
- implement validation against prompt injection