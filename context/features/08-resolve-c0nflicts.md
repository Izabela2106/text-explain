### 6. Resolve conflicts — programmatic

write function that:

- Handle overlapping spans (prefer longer phrase, or earliest start)
- Cap the number of annotations shown in the UI
- Sort annotations by `start` for stable rendering
